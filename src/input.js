export function setupInput(_canvas, _gameState, _dispatchCommand) {
  // Phase 1 intentionally leaves input behavior unimplemented.
  // This module exists to preserve the command-based architecture.
}
